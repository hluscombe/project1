<div>
  <h1>All Users</h1>
  <% @users.each do |user| %>
  <li>
    <%= user.email %>
    <p>
      <%= link_to "Delete User Account", user_path(user), :method => 'delete', :data => {:confirm => 'Are you sure?'}, :class => 'btn btn-primary'  %>
      <% end %>
    </p>
    <br>
  </li>
</div>
